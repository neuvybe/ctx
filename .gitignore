# build output + local go cache
/bin/
.gocache/

# release artifacts (built by tools/build-binaries.mjs / prepare-release.mjs)
dist/

# node tooling devDeps (root package-lock.json IS committed so CI `npm ci` works)
node_modules/
.husky/_/

# OS junk
.DS_Store