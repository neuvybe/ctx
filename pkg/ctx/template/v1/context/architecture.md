# Architecture — {{PROJECT}}

> **Prompts — answer against the actual code, then delete this block.**
> - What are the entrypoints / CLI subcommands / server endpoints?
> - Trace one full end-to-end flow (ASCII diagram or numbered).
> - What are the package/module responsibilities (one line each)?
> - What's the concurrency model? (locks, singleflight, worker pools, lock
>   ordering)
> - What integration paths exist (active vs latent)? Any that look active but
>   aren't?
> - Any git/CI/external-service integration worth noting?

## Entrypoints / subcommands

> Fill in.

## End-to-end flow

> Fill in (diagram or numbered).

## Package responsibilities

> Fill in (one bullet per package/module with its role).

## Concurrency model at a glance

> Fill in (which mutexes guard what; singleflight/pools; lock ordering; caches).

## Integration paths

> Fill in (active vs latent; note anything that looks active but isn't).

## Git / CI / external integration

> Fill in.