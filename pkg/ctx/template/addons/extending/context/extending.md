# Extension points — {{PROJECT}}

<!-- ctx:doc {"status":"draft","verifiedAt":"","sources":[]} -->

> **Document supported extension seams, not every place code could change, then
> delete this note.** Keep this document near 800 words. For `verified`, set
> `verifiedAt` to `<commit-hash> @ YYYY-MM-DD` and list the defining sources. Use
> `not-applicable` with a reason when the project has no formal extension API.
> Defer to a canonical owner-authored guide instead of duplicating it.

## Extension inventory

| Extension point | Contract | Registration/wiring | Representative implementation | Required tests | Caveats |
|---|---|---|---|---|---|
| | | | | | |

## Addition procedure

> For each supported seam, list the smallest end-to-end steps needed to add a
> capability and verify it. Point to owner instructions for general coding
> conventions rather than copying them here.
