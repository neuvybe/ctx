# Glossary — {{PROJECT}}

<!-- ctx:doc {"status":"draft","verifiedAt":"","sources":[]} -->

> **Define only terms whose project meaning is ambiguous or non-standard, then
> delete this note.** Keep this document near 800 words. For `verified`, set
> `verifiedAt` to `<commit-hash> @ YYYY-MM-DD` and list the repo-relative sources
> used. Use `not-applicable` with a reason when ordinary language is sufficient.
> Write this after the other fact documents.

| Term | Project-specific meaning | Source | Not to be confused with |
|---|---|---|---|
| | | | |
