# Contracts and compatibility — {{PROJECT}}

<!-- ctx:doc {"status":"draft","verifiedAt":"","sources":[]} -->

> **Fill only for real compatibility boundaries, then delete this note.** Keep
> this document near 800 words. For `verified`, set `verifiedAt` to
> `<commit-hash> @ YYYY-MM-DD` and list the defining repo-relative sources. Use
> `not-applicable` with a short reason when the project has no durable or external
> representation contract.

## Contract inventory

| Boundary | Owner/source | Producer → consumer | Shape/version | Compatibility promise |
|---|---|---|---|---|
| | | | | |

## Invariants and validation

> Record identity, ordering, consistency, freshness, and validation rules that
> consumers rely on. Include a representative example only when it clarifies a
> non-obvious contract.

## Change and migration procedure

> Explain how a contract can change safely: backward/forward compatibility,
> version negotiation, rollout order, migration, rollback, and required tests.
